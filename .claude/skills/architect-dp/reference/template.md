# {과제명} 설계포인트 전개 — DP{X} · DP{Y} (v0.1)

작성 기준: {기준 아키텍처 문서/다이어그램 + 버전}. QA 정의·별점 기준은 [{QA 정의 문서}]({경로})를 따른다.

<!-- 문서 머리 필수 2요소: ①작성 기준(입력 아키텍처 버전) ②QA 정의 문서 링크.
     QA 정의 문서가 없으면 architect-qa 스킬로 먼저 생성한다 — DP 문서 안에
     QA 표를 복제하지 않는다 (단일 출처 유지). -->

---

## 0. QA 정의 (참조)

QA1–QA{N}의 정의·측정 방법·별점별 정량 bin·근거는 [{QA 정의 문서}]({경로}) 참조.
본 문서의 모든 QA 평가표 별점은 해당 문서의 bin 기준으로 해석한다.

---

## DP{X}. {설계 포인트 이름 — "무엇을 결정하는가"가 드러나게}

### 문제 정의

<!-- 규칙:
  - 양방향 압력을 모두 서술 (A안 쪽으로 미는 힘 + B안 쪽으로 미는 힘).
  - 구체 근거 3개 내외, 핵심 어구 bold, 실측/논문 계열 결과 인용.
  - 가능하면 고전 문제와의 동형성 anchoring ("X의 고전 논쟁과 동형"). -->

{현 구조의 전제와 그것이 깨지는 지점. 구체 근거 목록:}

- **{근거 1 키워드}**: {구체 서술}
- **{근거 2 키워드}**: {구체 서술}
- **{근거 3 키워드}**: {구체 서술}

{반대 방향의 압력 문단.}

### 설계 쟁점

<!-- 후보 선택을 실제로 가르는 질문만. 마지막은 반드시 타 DP 커플링. -->

1. {쟁점 1 — 표현 가능성/핵심성 질문}
2. {쟁점 2 — 반응성/자율성 수준 질문}
3. {쟁점 3 — 독립성/재사용성 질문}
4. (DP{Z} 커플링) {타 DP 채택안이 본 DP의 실현 가능 집합을 어떻게 제약하는가}

### 후보구조 1 — {순수형 이름 (별칭)}

<!-- 순수형만. hybrid는 검토 노트로. -->

**구조**: {어느 패키지에 무엇이 생기고, 누가 무엇을 담당하는지 — 컴포넌트 수준}

**주의 — {혼동 소지 변형}과의 구분**: {필요 시. 예: plugin형 vs fork형} <!-- 선택 -->

**장점**
- {장점 — 아래 QA표의 가점 근거와 대응}
- …

**단점**
- {단점 — 아래 QA표의 감점 근거와 대응}
- …

**QA 평가**

<!-- 별점은 QA 정의 문서의 정량 bin으로 판정 (architect-qa 스킬 참조).
     모든 셀에 "어느 bin에 왜 해당하는지" + 근거 등급(A/B/C) + 시점(F/M).
     상한/도달 리스크 분리 표기 가능: ★★★ (상한) / ★☆ (도달 리스크) -->

| QA | 평점 | 정량 근거 |
|----|------|-----------|
| QA1 | ★★☆ (F) | {어느 bin에 왜 — 수치/범위/리드타임 + 근거등급} |
| QA2 | | |
| QA3 | | |
| QA4 | | |
| QA5 | | |

### 후보구조 2 — {순수형 이름}

**구조**: {…}

**장점**
- …

**단점**
- …

**QA 평가**

| QA | 평점 | 정량 근거 |
|----|------|-----------|
| QA1 | | |
| QA2 | | |
| QA3 | | |
| QA4 | | |
| QA5 | | |

### 검토 노트

<!-- 필수 3요소:
  ① 실질 결정 변수 한 문장 ("두 후보의 실질 결정 변수는 …인가다").
  ② hybrid 채택 방향 (있다면) + 왜 후보로 세우지 않았는지.
  ③ 진화 경로형 결정 (유효하다면): 시작 후보 + 전환 트리거(ADR 목록화 조건). -->

- {실질 결정 변수}
- {hybrid / 진화 경로}

---

## DP{Y}. {…}

<!-- DP{X}와 동일 구조 반복 -->

---

## DP 간 의존성

| 의존 | 내용 |
|------|------|
| DP{X} → DP{Y} | {X 채택안이 Y의 실현 가능 집합을 어떻게 제약} |
| DP{Y} → 구조도 | {채택 시 해소되는 구조도 미확정 주석} |
| 공통 | {양 DP가 전제하는 컴포넌트/조건} |
