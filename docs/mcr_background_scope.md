# MCR 과제 — 배경 · 필요성 · 범위 (v5)

변경 이력: v5 — **과제 목표 재정의 및 단계화(MCR 1단계)**. 과제 범위를
"메모리 중심 런타임 설계" 전반에서 **KV 캐시 최적 운용 AI 런타임 설계**로
특정: ① KV 재사용성 제고로 지연시간 개선 ② LLM 정확도를 유지한 KV 압축으로
메모리 병목 해소(지연·처리량 개선) ③ KV 캐시 인지형 동적 스케줄링으로 처리량
개선 — 의 3목표 체계로 재편. 자사 memory-centric 디바이스(PIM/PNM·CXL·HBF)
1급 통합과 근접연산 오프로드(SSD-PIM retrieval 가속 포함)는 **2단계(MCR
완성) 진화 경로로 이관** — 1단계는 commodity tier(GPU HBM + DRAM·SSD)에서
KV 운용 정책·구조를 입증하고, 그 정책 계층이 2단계 이종 디바이스 활용의
전제 조건이 된다(§2.3·§3 참조). v4 — 배경 1을 "① AI Agent의 특징(장기 기억 요구→컨텍스트 폭증) →
② 메모리 병목 심화(KV↑, HBM 초과 시 단순 offloading의 성능 저하) → ③ 업계
현황(메모리 제품군 등장 + 자사 포트폴리오)"으로 재구성. 병목의 출발점을
워크로드 변화(Agent화)에서부터 세워 "왜 컨텍스트가 폭증하는가"의 원인 서사를
보강. v3 — 배경 2에 "④ 성능은 런타임이 실현한다" 단을 신설해 ①~⑥ 6단
논리로 확장. 기존 ④(현 런타임 한계) 앞에 런타임 계층 자체의 중요성(결정
계층으로서의 역할, 결정 공간의 폭발, 동일 HW 실측 격차, 실현 계층으로서의 사업
함의)을 먼저 세워 "왜 갑자기 런타임인가"의 논리 비약을 해소. vLLM·FlexGen
실측은 ④의 근거로 이동. v2 — 배경을 ①~⑤ 5단 논리(배경1: 병목→업계 해법→자사
재료 / 배경2: 현 런타임 한계→필요성)로 재구성. "승부처의 이동" 추상 명제를
"현재 런타임의 한계"라는 검증 가능한 사실 명제로 대체하고, vLLM·FlexGen 실측
근거와 LMCache 비교를 편입. v1 — 배경·필요성·범위 병합안 최초 작성.

## 1. 과제 배경 1 — Agent 워크로드, 병목 심화, 그리고 업계의 재료

### 1.1 ① AI Agent의 특징 — 워크로드가 장기 기억을 요구한다

AI 활용의 중심이 단발 Q&A 챗봇에서 Agent로 이동하고 있다. Agent 워크로드에는
다음의 구조 변화가 필수적으로 일어난다:

- **Multi-turn loop이 기본 패턴**: 하나의 요청이 한 번의 추론으로 끝나지 않고,
  계획 → 실행 → 관찰 → 재계획의 루프를 수십~수백 스텝 반복한다.
- **툴 호출 기본 장착**: 매 스텝이 검색·코드 실행·외부 API 등 툴을 호출하고,
  그 결과물(문서·로그·데이터)이 컨텍스트로 계속 유입된다.
- **Sub-agent 시스템**: 하나의 목적을 위해 여러 sub-agent가 분업·병렬 실행하고
  산출물을 모으는 구조가 표준이 되고 있다.

세 변화의 공통 귀결은 **장기 기억 요구**다. 루프의 매 스텝이 이전 대화·툴
결과·sub-agent 산출물을 참조해야 하고, 이 기억은 세션을 넘어 사용자·과제
단위로 쌓인다. 즉 Agent화는 **컨텍스트 길이의 폭증**이라는 문제를 반드시 풀
것을 요구한다.

### 1.2 ② 이 변화가 메모리 병목을 심화시킨다

①의 컨텍스트 폭증은 추론의 지배 항목을 연산에서 메모리로 옮긴다.

- **컨텍스트 증가 → KV cache 증가**: KV cache는 컨텍스트 길이 × 동시 세션 수에
  비례해 커진다. 게다가 decode는 매 토큰마다 누적된 KV 전체를 읽는
  memory-bandwidth-bound 연산이라(자체 P/D 분리 벤치 실측에서 decode 대기가
  end-to-end latency의 70–85%(A)) KV 증가는 용량과 대역폭을 동시에 압박한다.
- **HBM 초과 + 단순 offloading의 성능 저하**: 커진 KV는 HBM 용량을 초과하기
  시작한다. 이를 단순히 DRAM→SSD로 내리는 offloading은 tier마다 대역폭이
  ~10×씩 낮아지는 계단(HBM ~TB/s → DRAM ~100GB/s → SSD ~10GB/s)을 그대로
  노출해, 수동적 스왑만으로는 latency 급증·처리량 급락으로 이어진다.

### 1.3 ③ 업계 현황 — 메모리 중심 제품군의 등장

- **메모리 업계의 대응**: 이 병목을 하드웨어 차원에서 풀기 위한 제품군이
  등장하고 있다. 대역폭 축에는 HBM 고도화와 **근접 연산(PIM/PNM)** — 데이터를
  연산기로 가져오는 대신 연산을 데이터가 있는 곳으로 보내 이동량 자체를 줄인다.
  용량 축에는 **CXL 기반 확장·풀링과 새로운 중간 tier(HBF 등)** — HBM 밖의 크고
  싼 계층으로 용량을 확장한다. 메모리 계층은 5+ tier로 깊어지는 중이다.
- **자사(삼성)의 제품군**: 근접 연산(PIM·CIM 계열)부터 계층화(Custom
  HBM·HBF·CXL 기반 확장)까지, 두 축 모두에 걸친 memory-centric 제품
  포트폴리오를 보유·개발 중이다. 각 디바이스는 근접연산·대역폭·용량이라는 서로
  다른 축의 무기를 제공하며, 조합하면 GPU HBM 단일 체계로는 불가능했던 설계
  공간이 열린다. **즉 병목을 풀 하드웨어 재료는 갖춰지고 있다.**

## 2. 과제 배경 2 — 성능을 실현할 런타임이 없다

### 2.1 ④ AI 성능은 하드웨어가 아니라 런타임이 실현한다

②의 병목과 ③의 재료 사이에는 결정의 공백이 있다. 하드웨어는 성능의
**능력(capability)** 을 제공할 뿐, 성능 자체를 만들지 못한다.

- **런타임 = 성능을 결정하는 계층**: 모델(응용)은 "무엇을 계산할지"를, 디바이스는
  "무엇이 가능한지"를 정의할 뿐이다. 그 사이 — 어떤 데이터를 어디에 두고(배치),
  언제 옮기고(이동), 어떤 연산을 어디서 실행하고(연산 배치), 어떤 요청부터
  처리할지(스케줄링) — 는 하드웨어가 스스로 답할 수 없는 질문이며, 이 결정을
  내리는 계층이 런타임이다. 시스템이 실제로 도달하는 성능(goodput·latency·비용)은
  이 결정의 질로 정해진다.
- **결정 공간의 폭발 — 런타임의 비중이 커지는 이유**: GPU+HBM 단일 체계에서는 둘
  곳도 연산할 곳도 하나뿐이라 결정이 자명했고, 런타임은 얇아도 됐다. 그러나 ②로
  병목이 메모리로 이동하고 ③의 계층화·근접 연산 디바이스가 들어오는 순간
  선택지는 tier × 디바이스 × 요청의 조합 공간으로 폭발하며, 데이터 배치와 연산
  배치라는 두 결정은 서로 결합(coupled)되어 있고, 잘못된 정책은 baseline보다도
  느리다. **하드웨어가 이종화될수록 성능의 분산은 런타임 쪽으로 이동한다.**
- **실측 근거 — 같은 하드웨어, 런타임만으로 갈린 성능**: 메모리 관리
  방식(paging)을 고친 것만으로 같은 GPU에서 KV cache 낭비가 60–80% → <4%로
  줄고 처리량 2–4×가 갈렸으며(vLLM, SOSP'23), 같은 16GB GPU 한 장에서
  offloading 배치 정책만으로 최대 처리량 **100×** 차이가 났다(FlexGen, ICML'23
  vs Accelerate·ZeRO-Inference). **하드웨어 스펙은 성능의 상한을 정할 뿐, 실제
  도달 성능은 런타임이 정한다**는 실증이다.
- **사업 함의 — 디바이스 가치의 유일한 통로**: 디바이스의 가치는 런타임을
  통해서만 응용에 도달한다. 런타임이 지원하지 않는 하드웨어는 응용에서 보이지
  않는다 — GPU 생태계의 해자가 실리콘이 아니라 CUDA 계열 SW 스택이라는 것이
  방증이다. 따라서 ③의 재료가 성능이 되려면 런타임이라는 관문을 통과해야 한다.

### 2.2 ⑤ 그런데 현재의 런타임은 연산기 중심이다 — 이 재료를 활용하지 못한다

④에서 성능의 관문이 런타임임을 확인했다. 그런데 현재 런타임 지형을 보면 그
관문이 모두 연산기(GPU)를 중심으로 설계되어 있다:

- **GPU 중심 설계**: vLLM 등 기존 스택은 "연산은 GPU, KV는 GPU HBM"이라는 단일
  메모리 가정 위에 설계되어 있다. 메모리는 GPU의 부속물로 취급되어 PIM/PNM 같은
  근접 연산 디바이스는 표현할 자리가 없고, HBM 용량을 넘는 순간
  preemption·재계산·스왑으로 성능이 급락하며, offloading이 있어도 콜드 데이터를
  밀어내는 정적·수동적 부가 기능 수준이다. ④의 실측(KV 60–80% 낭비)이 보여주듯
  메모리를 1급으로 다루지 않는 런타임의 낭비는 이미 단일 tier에서도 크다 —
  이종 tier에서는 그 격차가 100× 급으로 벌어진다.
- **KV 계층의 등장과 그 한계**: 업계도 이 한계를 인지해 KV를 HBM 밖으로 내리는
  계층이 등장했다 — LMCache는 KV를 CPU DRAM·SSD·원격 저장소(Redis 등) 계층에
  영속화·재사용하고 압축(CacheGen)·비접두 재사용(CacheBlend)까지 갖춘, vLLM 공식
  KV offloading connector의 대표 구현이다. 그러나 이들 계층조차 **commodity
  저장소를 수동적 put/get 백엔드로 추상화**할 뿐이다: 디바이스의
  대역폭·지연·용량 특성을 인지하는 배치(topology-aware placement)도, 연산을
  데이터로 보내는 근접 연산도, 요청별 SLO·품질 예산 기반의 정책도 없다 — 데이터
  이동만 있고 연산 배치가 없으므로 대역폭 병목 축은 다루지 못한다.
- **스케줄러는 KV를 모른다(KV-blind)**: 현행 스케줄러(continuous batching)는
  KV를 "남은 블록 수"로만 본다. 어떤 요청이 어느 노드에 재사용 가능한 KV를
  갖고 있는지(locality), hit 시 prefill 비용이 얼마나 절감되는지를
  admission·라우팅이 고려하지 않아 — 재사용 가능한 KV가 있는 노드를 두고
  다른 노드로 라우팅하면 재사용의 이득이 통째로 사라진다. 메모리 압박 시의
  대응도 preemption 후 전체 재계산 또는 수동 스왑뿐 — "어떤 요청의 KV를
  압축/강등하면 전체 성능이 최대가 되는가"라는 선택 자체가 없다.

즉 문제 인식(KV는 HBM 밖으로)은 업계 공통이 되었으나, **재사용(KV 계층)·
압축(커널)·스케줄링(서빙 엔진)이 서로를 모르는 별개 계층으로 남아 있고 —
이 셋을 조율하는 계층도, 자사 메모리 디바이스가 1급 자원으로 들어갈 자리도
어느 런타임에도 없다.**

### 2.3 ⑥ 따라서 KV 캐시를 1급 관리 대상으로 다루는 런타임이 필요하다 — 단계적으로

③의 재료, ④의 관문, ⑤의 공백을 합치면 결론은 하나다: **KV 캐시를 일회성
버퍼가 아닌 1급 관리 대상(자산)으로 다루며, KV의 재사용·압축·배치와
스케줄링을 요청별 SLO·품질 예산 기준으로 조율하는 런타임**을 직접 개발해야
한다. 이것이 MCR(Memory-Centric Runtime)의 본체이며, 두 단계로 나눠 실현한다:

- **1단계 (본 과제) — KV 캐시 최적 운용 AI 런타임**: commodity tier(GPU HBM
  + DRAM·SSD) 위에서 ⑤의 세 공백 — 재사용·압축·스케줄링과 그 조율 계층 —
  을 채우는 런타임을 설계하고, GPU HBM 단일 tier baseline 대비 성능 개선을
  E2E 정량 입증한다.
- **2단계 (진화 경로) — 자사 memory-centric 디바이스 확장(MCR 완성)**: 1단계
  런타임의 tier 추상화(대역폭·지연·용량 파라미터)에 자사 디바이스(PIM/PNM·
  CXL·HBF·SSD-PIM)를 plug-in하고, 근접연산 오프로드(압축 연산·retrieval
  가속)를 편입해 자사 메모리 제품군의 **레퍼런스 소프트웨어 스택**으로
  완성한다.

**1단계가 2단계의 전제 조건이다**: ④에서 보였듯 하드웨어는 능력만 제공하고
성능은 런타임의 결정이 실현한다 — CXL·HBF 같은 하위 tier가 성능으로
전환되려면 "무엇을 내리고(배치), 얼마나 줄이고(압축), 언제 다시 쓰고(재사용),
어떤 요청부터 처리할지(스케줄링)"를 결정하는 정책 계층이 먼저 있어야 한다.
그 정책 계층의 관리 대상이 바로 KV 캐시다. KV 최적 운용 없이 이종 tier를
붙이면 FlexGen 실측이 보인 100× 격차의 나쁜 쪽에 서게 된다(④). 즉 1단계
산출물은 그 자체로 설계 과제의 결과물이며, 동시에 자사 메모리 사업의
소프트웨어 자산(2단계 레퍼런스 스택)으로 가는 관문이다.

## 3. 과제 범위 · 목적 (따라서 ~를 할 것이다)

### 3.1 목적 — KV 캐시 최적 운용 AI 런타임 (MCR 1단계)

LLM 추론에서 **KV 캐시를 1급 관리 대상으로 최적 운용하는 AI 런타임의
아키텍처를 설계**하고 그 효과를 E2E 정량 입증한다. 목표는 3축이다:

1. **KV 캐시 재사용성 제고 → 지연시간(TTFT) 개선**: prefix를 넘어
   비접두(chunk)·세션·사용자 단위까지 KV 재사용 범위를 넓히고, 복원 vs
   재계산을 비용 기준으로 판단해 재계산(re-prefill)을 회피한다.
2. **LLM 정확도를 유지한 KV 캐시 압축 → 메모리 병목 해소 → 지연·처리량
   개선**: 품질 저하 bound(QA2) 안에서 양자화·토큰 eviction 등 압축을
   요청별로 차등 적용해, 용량(동시 세션)과 대역폭(decode) 병목을 함께 푼다.
3. **KV 캐시 인지형 동적 스케줄링 → 처리량 개선**: cache-hit/locality를
   인지하는 admission·라우팅과 메모리 압박 시의 압축/강등/축출 선택을
   요청별 SLO 기준으로 수행해, 재사용·압축의 이득을 시스템 처리량으로
   전환한다.

세 축은 결합되어 있다 — 재사용은 "무엇이 남아 있나"를 바꾸고, 압축은
저장·이동 비용을 바꾸며, 스케줄링이 그 활용 여부를 결정한다. 이 **조율
계층**이 본 과제가 채우는 공백(⑤)이다. 2단계에서 본 런타임을 자사
memory-centric 디바이스의 레퍼런스 스택으로 확장한다(⑥).

### 3.2 범위 (In Scope)

1. **KV 최적 운용 런타임 레퍼런스 아키텍처 설계** (← 배경 ⑤·⑥): Inference
   Orchestration(control plane) / Inference Engine / Memory Engine(data
   plane)의 3-패키지 구조와 공개 인터페이스(KV Locator, CompressionOp) 정의.
   메모리 tier는 Tier Topology Model의 파라미터(대역폭·지연·용량)로
   추상화한다 — 1단계는 commodity tier(HBM·DRAM·SSD)로 구성하되, 이
   추상화가 2단계 자사 디바이스 plug-in의 접속점이 된다
2. **핵심 설계 결정(DP) 도출·평가** (← 배경 ⑤·⑥): DP1 실행 스택 소싱(외부
   스택 활용 vs 자체 구현), DP2 KV 배치·압축 관리 주체(중앙 정책 vs 자율),
   DP3 KV 재사용 범위·복원 전략 등 — 후보구조, QA 평가, ADR
3. **E2E 실행 경로 확보 및 정량 성능 증명** (← 배경 ①·②·⑥): 대표 워크로드
   (long-context RAG, multiturn, agent memory) 기준 E2E 벤치마크를 구축하고,
   GPU HBM 단일 tier baseline 대비 TTFT·throughput·유효 KV capacity·품질
   bound 개선을 실측 입증. 목표 3축의 순기여는 축별 ablation(재사용 off /
   압축 off / KV-blind 스케줄링)으로 분리 측정

### 3.3 범위 외 (Out of Scope)

- **메모리 디바이스 HW 설계 자체** — Tier Topology Model의 파라미터(대역폭·용량·지연)로만 취급
- **자사 memory-centric 디바이스 1급 통합·근접연산 오프로드** (PIM/PNM 연산
  오프로드, SSD-PIM retrieval 가속 등) — **2단계(MCR 완성) 진화 경로로
  이관**. 1단계는 tier 추상화 인터페이스로 접속점만 보존한다
- **모델 학습·재학습** — 정확도 유지는 training-free 조건에서 달성한다
  (모델 차원의 KV 축소[MLA 등]는 모델 설계 영역으로 범위 외)
- **클러스터 인프라 provisioning** — desired state 산출까지가 런타임 책임이며, 집행은 인프라 계층에 위임

### 3.4 QA 연결

목표 3축은 QA1(추론 성능 — 목표 1이 TTFT 축, 목표 2·3이 throughput 축)로
직결되며, 목표 2의 "정확도 유지"는 QA2(응답 품질 bound — gate)로, "메모리
병목 해소"는 QA3(메모리 효율)로 정량화된다. 조율 계층의 구조 요구는
QA4(확장성·진화성 — KV 구조 변화 수용 + 2단계 디바이스 수용 대비)로,
레퍼런스 스택 성립 요건은 QA5(개발·운영 비용)와 DP1의 판단 기준으로
계승된다. 상세 정의는 [00_qa_definitions.md](00_qa_definitions.md) 참조.

### 참고 문헌 (배경 ④·⑤ 근거)

- [vLLM — Efficient Memory Management for LLM Serving with PagedAttention (SOSP'23)](https://arxiv.org/abs/2309.06180) — KV 낭비 60–80% → <4%, 동일 latency 처리량 2–4×
- [FlexGen — High-Throughput Generative Inference of LLMs with a Single GPU (ICML'23)](https://arxiv.org/abs/2303.06865) — 동일 16GB T4, offloading 정책으로 최대 처리량 100×
- [LMCache — An Efficient KV Cache Layer for Enterprise-Scale LLM Inference](https://arxiv.org/abs/2510.09665) · [아키텍처 문서](https://docs.lmcache.ai/developer_guide/architecture.html) — CPU RAM/SSD/원격 백엔드, CacheGen/CacheBlend
